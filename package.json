{
  "name": "pocketcasts-electron",
  "version": "0.1.3",
  "license": "MIT",
  "main": "./dist/main.js",
  "build": {
    "appId": "com.pocketcasts.app",
    "files": [
      "./dist/**"
    ],
    "mac": {
      "category": "sound.pocketcasts"
    }
  },
  "scripts": {
    "assets": "copyfiles assets/** ./dist",
    "ci": "npm run compile && tsc && npm run lint && electron-builder --publish=never",
    "compile": "rimraf app/out",
    "lint": "tslint -c tslint.json -p tsconfig.json",
    "package": "tsc && electron-builder --publish=never",
    "start": "npm run assets && electron ./dist/main.js",
    "watch": "tsc -w"
  },
  "devDependencies": {
    "electron": "^4.0.0",
    "electron-builder": "^20.38.5",
    "electron-debug": "^2.0.0",
    "electron-rebuild": "^1.8.2"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/davegallant/pocketcasts_electron.git"
  },
  "dependencies": {
    "@types/electron-store": "^1.3.0",
    "copyfiles": "^2.1.0",
    "electron-log": "^3.0.1",
    "electron-store": "^2.0.0",
    "electron-window-state": "^5.0.2",
    "rimraf": "^3.0.0",
    "tslint": "^5.12.1",
    "typescript": "^3.2.4"
  },
  "optionalDependencies": {
    "dbus": "^1.0.3"
  }
}
