{
  "author": {
    "email": "davegallant@gmail.com",
    "name": "Dave Gallant"
  },
  "build": {
    "appId": "com.pocketcasts.app",
    "files": [
      "./dist/**"
    ],
    "linux": {
      "target": "deb"
    },
    "mac": {
      "category": "sound.pocketcasts"
    }
  },
  "dependencies": {
    "@types/node": "^12.7.12",
    "copyfiles": "^2.1.0",
    "electron-log": "^4.0.0",
    "electron-store": "^6.0.0",
    "electron-window-state": "^5.0.2",
    "rimraf": "^3.0.0",
    "tslint": "^6.0.0",
    "typescript": "^4.0.2"
  },
  "devDependencies": {
    "electron": "^11.0.1",
    "electron-builder": "^22.7.0",
    "electron-debug": "^3.1.0",
    "electron-rebuild": "^2.0.0"
  },
  "license": "MIT",
  "main": "./dist/main.js",
  "name": "pocketcasts-electron",
  "optionalDependencies": {
    "dbus-next": "^0.9.1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/davegallant/pocketcasts_electron.git"
  },
  "scripts": {
    "assets": "copyfiles assets/** ./dist",
    "build": "tsc && npm run assets && electron-builder --publish=never",
    "ci": "npm run compile && tsc && npm run lint && npm run build",
    "compile": "rimraf app/out",
    "lint": "tslint -c tslint.json -p tsconfig.json",
    "package": "npm run build",
    "postinstall": "electron-builder install-app-deps",
    "start": "tsc && electron ./dist/main.js",
    "watch": "tsc -w"
  },
  "version": "0.3.1"
}
